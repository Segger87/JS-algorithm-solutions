//Check if a value is classified as a boolean primitive. Return true or false.

//typeof returns what type of input something is (string, integer, boolean )
function booWho(bool) {
  return typeof bool === 'boolean';
}

booWho(null);
